export default function GrantsHeader({ text }: { text: string }) {
  return (
    <h3 className="text-active-white font-poppins font-bold text-[2rem] lg:text-[3rem] w-full">
      {text}
    </h3>
  );
}
